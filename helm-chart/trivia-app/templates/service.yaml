apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.appname }}"
spec:
  selector:
    app: "{{ .Values.appname }}"
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
